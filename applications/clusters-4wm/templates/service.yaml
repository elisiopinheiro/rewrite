apiVersion: v1
kind: Service
metadata:
  name: {{ include "clusters-4wm.fullname" . }}
  labels:
    {{- include "clusters-4wm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clusters-4wm.selectorLabels" . | nindent 4 }}
