apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "clusters-metadata-api.fullname" . }}-db-credentials
spec:
  refreshInterval: 10m0s
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "clusters-metadata-api.fullname" . }}-db-credentials
  data:
  - secretKey: password
    remoteRef:
      key: applications/clusters-metadata-api-{{ .Values.environment }}/database
      property: password
  - secretKey: username
    remoteRef:
      key: applications/clusters-metadata-api-{{ .Values.environment }}/database
      property: username
