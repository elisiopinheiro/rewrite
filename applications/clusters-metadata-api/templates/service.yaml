apiVersion: v1
kind: Service
metadata:
  name: {{ include "clusters-metadata-api.fullname" . }}
  labels:
    {{- include "clusters-metadata-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clusters-metadata-api.selectorLabels" . | nindent 4 }}
